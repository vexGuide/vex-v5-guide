int setValue(int var) { //define a function with return type “int”
    return var; //returns an integer (var)
    //end function
}

int main(void) {
    int a = setValue(5); //calls setValue, gives it 5, and the function returns 5, setting a to 5
    return 0; //end program
}
